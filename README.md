# Finance Agreement PoC

Tool to extract structured data from scanned finance agreement documents.

## Setup

1.  **Install Dependencies**
    ```bash
    pip install -r requirements.txt
    ```

2.  **OCR Engine**
    Ensure Tesseract OCR is installed on your system.
    - **macOS**: `brew install tesseract`
    - **Ubuntu**: `sudo apt-get install tesseract-ocr`
    - **Windows**: Download and install the Tesseract installer.

## Usage

(Coming soon)

## Structure

- `src/`: Source code for extractor, validator, and exporter.
- `tests/`: Unit tests.
- `examples/`: Sample documents (e.g., `IMG_1805.png`).
